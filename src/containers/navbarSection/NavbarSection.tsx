import { SectionBox } from "../../components"
import {Navbar} from "../../components"

const NavbarSection = () => {
  return (
    <SectionBox>
      <Navbar />
    </SectionBox>
  )
}

export default NavbarSection